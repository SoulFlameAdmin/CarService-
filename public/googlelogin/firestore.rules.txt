rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // всеки потребител може да чете/пише САМО своя профил
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }

  }
}
